import type { NextConfig } from "next";

// This file contains the configuration settings for a Next.js application.
// The nextConfig object holds various configuration options for the application.

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
